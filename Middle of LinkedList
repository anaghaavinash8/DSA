class Solution {
public:
    ListNode* middleNode(ListNode* head) {
        ListNode *one = head;  
        ListNode *skip = head;  

        while (skip != NULL && skip->next != NULL) {
            one = one->next;         
            skip = skip->next->next;   
        }

        return one;    
    }

    
};
